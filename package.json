{
  "name": "simple-web-client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start-nginx": "npx tsc --project nginx-ts --watch",
    "build-nginx": "npx tsc --project nginx-ts",
    "start-reload-server": "npx tsc --project reload-server-ts --watch",
    "build-reload-server": "npx tsc --project reload-server-ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "npx prettier . --write"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/poold3/simple-web-client"
  },
  "author": "Dallin Pool",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/ws": "^8.18.1",
    "prettier": "3.5.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "ws": "^8.18.2"
  }
}
