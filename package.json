{
  "name": "simple-web-client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx tsc --project lib && npx tsc --project nginx-src && npx tsc --project reload-server-src",
    "lib": "npx tsc --project lib --watch",
    "nginx": "npx tsc --project nginx-src --watch",
    "reload-server": "npx tsc --project reload-server-src --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "npx prettier . --write"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/poold3/simple-web-client"
  },
  "author": "Dallin Pool",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/ws": "^8.18.1",
    "prettier": "3.5.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "ws": "^8.18.2"
  }
}
